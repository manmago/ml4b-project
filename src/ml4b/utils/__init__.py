"""Utils subpackage for ML4B gym exercise recognition.

Provides shared utilities used across the package:
- config.py: centralised path resolution via environment variables (.env)

Import paths from config rather than hardcoding them anywhere in the project.
"""
