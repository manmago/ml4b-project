"""Streamlit web application package for ML4B gym exercise recognition.

Making ``app`` a package lets the entry point import its sub-pages with
``from app.pages.<name> import render`` when the app is launched from the
project root via ``uv run streamlit run app/streamlit_app.py``.
"""
