# Data

This folder is in .gitignore — data is NOT committed to git.

## Structure
- `raw/`       → Original sensor data (Apple Watch CSV exports), never edit
- `processed/` → Preprocessed features

## Getting the Data
Ask Anshul for the shared folder link (OneDrive/Google Drive).
